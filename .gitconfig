# This is Git's per-user configuration file.
[user]
	name = zno
	email = zno@iMac.local

[alias]
    lg = log --graph --decorate --oneline
    ll = log --graph --pretty=format:'%C(auto)%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cd) %C(bold blue)<%an>%Creset'

